import 'package:flutter/material.dart';

class AppColors{ //ADD COLORS HERE AND USE THEM IN THE OTHER FILES
  static const primary = Colors.black;
  static const secondary = Color.fromARGB(255, 50, 50, 50);
  static const background = Colors.white;
  static const highlighted = Color.fromARGB(255, 172, 20, 20);
  static const priceColor = Color.fromARGB(255, 96, 96, 96);
} //These are the most used colors, you don't have to add every color here